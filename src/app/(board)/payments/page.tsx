import React from 'react';
import PaymentsList from "@/features/PaymentsList/PaymentsList";

const Page = () => {
    return (
        <div>
            <PaymentsList/>
        </div>
    );
};

export default Page;