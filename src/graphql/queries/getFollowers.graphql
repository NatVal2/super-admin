query getFollowers(
    $userId: Int!,
    $pageSize: Int!,
    $sortBy: String!,
    $sortDirection: SortDirection,
    $pageNumber: Int!
){
    getFollowers(
        userId: $userId,
        pageSize: $pageSize,
        sortBy: $sortBy,
        sortDirection: $sortDirection,
        pageNumber: $pageNumber
    ){
        items{
            id
            userId
            userName
            createdAt
        }
        pagesCount
        page
        pageSize
        totalCount
    }
}